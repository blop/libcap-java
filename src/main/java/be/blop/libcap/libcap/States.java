package be.blop.libcap.libcap;

/**
 * These are the states available to each capability
 */
public interface States {
	int CAP_CLEAR = 0;			/* The flag is cleared/disabled */
	int CAP_SET = 1;			/* The flag is set/enabled */
}